// Phase 0 skeleton — 完整实现在 Phase 3+ 按需激活

export function DetailDrawer() {
  return (
    <div className="rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] p-8 text-center text-sm text-[var(--color-text-tertiary)]">
      DetailDrawer — Phase 3+ 激活
    </div>
  );
}
